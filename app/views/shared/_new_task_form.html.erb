<%= form_for [project, @new_task], remote: true do |form| %>
		<%= form.hidden_field :project_id, value: project.id %>
		<div class="input-append">
		<%= form.text_field :title %>
		<button type="submit" class="btn">Create Task</button>
		</div>
<%end%>